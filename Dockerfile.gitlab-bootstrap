FROM docker:27-cli

RUN apk add --no-cache bash curl git python3 py3-requests

WORKDIR /workspace

COPY scripts/gitlab/bootstrap_gitlab.sh /bootstrap/bootstrap_gitlab.sh

ENTRYPOINT ["/bin/bash", "/bootstrap/bootstrap_gitlab.sh"]
